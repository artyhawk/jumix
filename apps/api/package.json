{
  "name": "@jumix/api",
  "version": "0.0.0",
  "private": true,
  "description": "Jumix API — Fastify backend (REST + OpenAPI)",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "echo 'TODO: tsx watch src/server.ts'",
    "build": "echo 'TODO: tsc --project tsconfig.json'",
    "start": "node dist/server.js",
    "typecheck": "echo 'TODO: tsc --noEmit'",
    "test": "echo 'TODO: vitest run'"
  }
}
